import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int T = sc.nextInt();

        while (T-- > 0) {
            String s = sc.next();

            int n = s.length();
            int[] freq1 = new int[26];
            int[] freq2 = new int[26];

            // Count characters in the first and second halves
            for (int i = 0; i < n / 2; i++) {
                freq1[s.charAt(i) - 'a']++;
                freq2[s.charAt(n - 1 - i) - 'a']++;
            }

            boolean isLapindrome = true;

            // Compare frequencies
            for (int i = 0; i < 26; i++) {
                if (freq1[i] != freq2[i]) {
                    isLapindrome = false;
                    break;
                }
            }

            System.out.println(isLapindrome ? "YES" : "NO");
        }

        sc.close();
    }
}
